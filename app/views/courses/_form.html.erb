<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_area :descricao %>
  </div>

  <div class="field">
    <%= form.label :horas_aula %>
    <%= form.number_field :horas_aula %>
  </div>

  <div class="field">
    <%= form.label :vagas_total %>
    <%= form.number_field :vagas_total %>
  </div>

  <div class="field">
    <%= form.label :vagas_ocupadas %>
    <%= form.number_field :vagas_ocupadas %>
  </div>

  <div class="field">
    <%= form.label :data_inicio %>
    <%= form.date_select :data_inicio %>
  </div>

  <div class="field">
    <%= form.label :data_fim %>
    <%= form.date_select :data_fim %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
